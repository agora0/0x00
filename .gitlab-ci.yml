image: ruby:2.6
variables:
  JEKYLL_ENV: production
# cache:
#   paths:
#     - vendor/
# before_script:
#   - bundle install --path vendor
pages:
  stage: deploy
  script:
    - bundle exec jekyll build -d _site/
  artifacts:
    paths:
      - _site
  only:
    - master
